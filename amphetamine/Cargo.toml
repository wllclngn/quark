[package]
name = "triskelion"
version = "0.10.2"
edition = "2024"
license = "GPL-2.0-only"
description = "Lock-free wineserver replacement for amphetamine"

[[bin]]
name = "triskelion"
path = "src/main.rs"

[dependencies]
libc = "0.2"

[build-dependencies]
